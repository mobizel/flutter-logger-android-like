name: flutter_logger_android_like
version: 1.0.0

description: A simple logger similar to Android devices one
homepage: https://github.com/mobizel/flutter-logger-android-like

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  ansicolor: ^1.0.2
  logging: ^0.11.4

